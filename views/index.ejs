<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/main.css">
  <title>Weather App</title>
</head>

<body>
    <h1>Hello World</h1>
    <% if (data && data.daily.weather && data.daily.weather.length > 0) { %>
      <p>Weather: <%= data.daily.weather[0].main %></p>
      <p>Description: <%= data.daily.weather[0].description %></p>
    <% } else { %>
      <p>Weather data not available.</p>
    <% } %>
  </body>
  </html>